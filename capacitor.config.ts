import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'osagarria_ep',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
